A simple Alpine container with the [Crowdsec Firewall Bouncer](https://github.com/crowdsecurity/cs-firewall-bouncer) binary and `nftables`.
Suitable for VMs like those in GCE that use Google's Container-Optimized OS (COS).
See [sample compose file](compose.yaml).
